#!/bin/bash

ASCPATH="${1}"
TXTPATH="${ASCPATH%.asc}.txt"

if [[ "${ASCPATH}.txt" == "${TXTPATH}" ]] ; then
	echo "Invalid file name: $ASCPATH" 1>&2
	exit 1
fi

if ! pytnon3 -m myenc decred -o "${TXTPATH}" "${ASCPATH}" ; then
	echo "Failed to decrypt ${ASCPATH} -> ${TXTPATH}" 1>&2
	exit 1
fi

rm -f "${ASCPATH}"
